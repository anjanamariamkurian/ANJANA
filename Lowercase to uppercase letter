#include<iostream.h>
#include<stdio.h>
#include<string.h>
int main()
{
	char str[10];
	int i;
	cout<<"enter any string";
	cin>>str;
	for(i=0;i<=strlen(str);i++)
	{
		if(str[i]>=65&&str[i]<=90)
		{
			str[i]=str[i]+32;
		}
	}
	cout<<"lowercase string:"<<str;
	return 0;
}
