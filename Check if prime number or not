#include<iostream.h>
#include<stdio.h>
int main()
{
	int i,flag=0,n;
	cout<<"Enter any number";
	cin>>n;
	for(i=2;i<n;i++)
	{
		if(n%i==0)
		{
			flag=1;
			break;
		}
	}
	if(flag==0)
	{
		cout<<"PRIME NUMBER";
	}
	else
	{
		cout<<"NOT A PRIME NUMBER";
	}
	return 0;
}
