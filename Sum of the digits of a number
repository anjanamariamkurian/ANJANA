#include<iostream.h>
#include<conio.h>
int main()
{
	int rem,sum=0,num;
	cout<<"enter any number";
	cin>>num;
	while(num>0)
	{
	      rem=num%10;
	      sum=sum+rem;
	      num=num/10;
	}
	cout<<"Sum of digits="<<sum;
	return 0;
}
