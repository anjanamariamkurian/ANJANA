#include<stdio.h>
#include<iostream.h>
int main()
{
	int range,i,sum=0;
	cout<<"enter the range";
	cin>>range;
	sum=(range*(range+1))/2;
	cout<<"sum of the series:";
	for(i=1;i<=range;i++)
	{
		if(i!=range)
			cout<<i<<"+";
		else
			cout<<i<<"="<<sum;
	}
	return 0;
}
