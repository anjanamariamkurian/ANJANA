#include<iostream.h>
#include<stdio.h>
#include<math.h>
int main()
{
	int i,range,sum=0;
	cout<<"enter the range";
	cin>>range;
	sum=pow(((range*(range+1))/2),2);
	cout<<"sum:";
	for(i=1;i<=range;i++)
	{
		if(i!=range)

			cout<<i<<"^3+";
		else
			cout<<i<<"^3="<<sum;
	}
	return 0;
}
