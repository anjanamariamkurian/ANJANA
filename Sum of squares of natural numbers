  #include<stdio.h>
  #include<iostream.h>
  int main()
  {
  int range,i,sum=0;
  cout<<"enter range";
  cin>>range;
  sum=(range*(range+1)*(2*range+1))/6;
  cout<<"sum:";
  for(i=1;i<=range;i++)
  {
	if(i!=range)
		cout<<i<<"^2+";
	else
		cout<<i<<"^2="<<sum;
  }
  return 0;
  }
